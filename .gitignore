# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor junk
.vscode/
.idea/
*.swp
*.swo
*~

# Backups from in-place sed / awk / similar
*.bak
*.orig

# OneDrive sync conflicts
*conflict*

# Python (service/)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local env files (NEVER commit these)
.env
.env.local
.env.*.local
service/.env

# Supabase CLI local cache (project-config commits via supabase/config.toml)
**/supabase/.temp/
**/supabase/.branches/

# Local lead JSON files used by manual playbook 06
lead-*.json
Assessment - * - *.pdf

